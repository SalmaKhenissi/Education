{{ form_start(form) }}
    <div class="row">
       <div class="col-md-4">{{ form_row(form.libelle)}}</div>
     </div>
     <div class="row">
       <div class="col-md-4">{{ form_row(form.shortcut)}}</div>
     </div>
     <div class="row">
       <div class="col-md-4">{{ form_row(form.levels)}}</div>
     </div>
     
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}

