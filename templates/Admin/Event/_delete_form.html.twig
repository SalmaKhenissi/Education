<form method="post" action="{{ path('admin_event_delete', {'id': e.id}) }}" onsubmit="return confirm('Est ce que t'es sùr de supprimer cet événement ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e.id) }}">
    <button class="label label-danger btn-xs">Supprimer</button>
</form>
