
<form method="post" action="{{ path('admin_guardian_delete', {'id': g.id}) }}" onsubmit="return confirm('Est ce que t'es sùr de supprimer ce parent ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ g.id) }}">
    <button class="btn btn-danger fa fa-trash"></button>
</form>

