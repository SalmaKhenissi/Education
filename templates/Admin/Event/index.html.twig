{% extends 'admin-base.html.twig' %}


{% block body %}
<!--== BODY INNER CONTAINER ==-->
            <div class="sb2-2">
                <!--== breadcrumbs ==-->
                <div class="sb2-2-2">
                    <ul>
                        <li><a href="{{path('admin_dashbord')}}"><i class="fa fa-home" aria-hidden="true"></i> Acceuil</a>
                        </li>
                        <li class="active-bre"><a href="#"> Evenements</a>
                        </li>
                        <li class="active-bre"><a href="#"> Liste des événements</a>
                        </li>
                    </ul>
                </div>

                <!--== User Details ==-->
                <div class="sb2-2-3">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="box-inn-sp">
                                <div class="inn-title">
                                    <h4>Evenements</h4>
                                </div>
                                <div class="tab-inn">
                                    <div class="table-responsive table-desi">
                                        <table class="table table-hover">
                                            <thead>
                                                <tr>
                                                    <th>Titre</th>
                                                    <th>Image</th>
													<th>Location</th>
                                                    <th>Temps</th>
													<th>Action</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                            {% for e in events %}
                                                <tr>
                                                    <td>{{e.title}}</td>
                                                    <td><img src="{{'/Pro-Education/public' ~ vich_uploader_asset( e ,'imageFile') }} " alt="picture" style="width:30%; "></span></td>
                                                    <td>{{e.location}}</td>
                                                    <td>{{ e.time ? e.time|date('Y-m-d H:i') : '' }}</td>
                                                    <td>
                                                        <a href="{{ path('admin_event_show', {'id': e.id}) }}" ><span class="fa fa-eye">Voir</span></a><br>
													    <a href="{{path('admin_event_edit' , {'id':e.id})}}" ><span class="fa fa-pencil"> Modifier</span></a>
                                                        {{ include('Admin/Event/_delete_form.html.twig') }}
                                                     </td>
                                                </tr>
                                                {% endfor %}
                                                
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
{% endblock %}