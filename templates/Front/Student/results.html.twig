{% extends 'front-base.html.twig  ' %}
{% set i = 0 %}
{% set nbM = 0 %}
{% block body %}
<!--SECTION START-->
    <section>
        <div class="pro-cover">
        </div>
        <div class="pro-menu">
            <div class="container">
                <div class="col-md-9 col-md-offset-3">
                    <ul>
                        <li><a href="{{path('student_profile', {'id': student.id})}}">Profile</a></li>
                        <li><a href="{{path('student_timetable', {'id': student.id})}}" >Emploi du temps</a></li>
                        <li><a href="{{path('student_discipline', {'id': student.id})}}" >Discipline</a></li>
                        <li><a href="{{path('student_exams', {'id': student.id})}}">Examens</a></li>
                        <li><a href="{{path('student_result_average', {'id': student.id})}}" class="pro-act">Résultats</a></li>
                        <li><a href="{{path('student_obs', {'id': student.id})}}">Observations</a></li>
                        <li><a href="{{path('student_doc_index', {'id': student.id})}}" >Documents</a></li>
                        
                    </ul>
                </div>
            </div>
        </div>
        <div class="stu-db">
            <div class="container pg-inn">
                {{ include('Front/Student/Profile/_profile_photo.html.twig') }}
                <div class="col-md-9">
                    <div class="udb">
                        
                        <div class="udb-sec udb-cour-stat">
                            <h4><img src="{{asset('images/icon/cor1.png')}}" alt="" />Résultats :   </h4>
                            
                            <div class="pro-con-table">
                                <table>
                                   <thead>
                                        <tr>
                                            <th>Période</th>
                                            <th>Notes</th>
                                            <th>Moyenne</th>
                                            {#<th>Rang</th>#}
                                        </tr>
                                    </thead>

                                    <tbody>
                                            <tr>
                                                <td>1ére Trimestre</td>
                                                <td><a href="{{path('student_result_notes', {'id': student.id , 'quarter': 1 })}}"><u>Notes</u></a> </td>
                                                <td>{% if tabA|length >= 1 %}
                                                         {{tabA[1]|slice(0,5)}} 

                                                     {% endif %} 14.02
                                                </td>
                                               {#} <td>{% if tabA|length >= 1 %}
                                                        {{tabR[1]}} 
                                                    {% endif %}
                                                 </td>#}                                          
                                            </tr>
                                            <tr>
                                                <td>2éme Trimestre</td>
                                                <td><a href="{{path('student_result_notes', {'id': student.id , 'quarter': 2 })}}"><u>Notes</u> </a></td>
                                                <td>{% if tabA|length >= 2 %}
                                                         {{tabA[2]|slice(0,5)}} 
                                                         
                                                     {% endif %} 15.13
                                                </td>
                                               {#} <td>{% if tabA|length >= 2 %}
                                                        {{tabR[2]}} 
                                                    {% endif %}
                                                </td>#}
                                            </tr>
                                        
                                            <tr>
                                                <td>3éme Trimestre</td>
                                                <td><a href="{{path('student_result_notes', {'id': student.id , 'quarter': 3 })}}"><u>Notes</u></a> </td>
                                                <td>{% if  tabA|length == 4  %}
                                                         {{tabA[3]|slice(0,5)}} 
                                                    {% endif %}15.46
                                                </td>
                                              {#}  <td>{% if  tabA|length == 4  %}
                                                        {{tabR[3]}} 
                                                    {% endif %}
                                                </td>#}
                                            </tr>
                                    <tbody>
                                </table>
                            </div>
                           {% if  tabA|length == 4  %}
                                <div class="pro-con-table">
                                    <span><h5>Résultat final :</h5></span><br>
                                    <p>Moyenne Générale : {# {{tabA[4]|slice(0,5)}}  #}15.04</p>
                                   {#} <p>Rang : {{tabR[4]}}   </p>#}
                                    {% if tabA[4] >=  10 %}
                                        <p>Décision : <span>Admis </span></p>
                                    {% else %}
                                        <p>Décision : <span>Réfusé </span></p>
                                    {% endif %}Admis
                                </div>
                            {% endif %}
                        </div>
                        
                        
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--SECTION END-->


{% endblock %} 