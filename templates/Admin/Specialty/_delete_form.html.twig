<form method="post" action="{{ path('admin_specialty_delete', {'id': specialty.id}) }}" onsubmit="return confirm('Est ce que t'es sùr de supprimer cette spécialtié ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialty.id) }}">
    <button class="btn">Delete</button>
</form>
